# Map static resources from the /public folder to the /assets URL path
GET         /libra             controllers.LibraController.helloWorld
POST         /rank             controllers.LibraController.processInfoUnits
GET         /register          controllers.LibraController.registerNewUser
GET         /all          controllers.LibraController.getAllUnitsForUser
